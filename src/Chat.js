import React, { Component } from 'react';
class Chat extends Component {
  render () {
    return (
      <div>
        <div>类似 &lt;Contacts /&gt;和 &lt;Chat /&gt;这样的 React 元素都是对象，所以你可以像任何其他元素一样传递它们</div>
        这是Chat组件
      </div>
    )
  }
}
export default Chat;